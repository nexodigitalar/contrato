/* Styles & Img */
import "./Spinner.scss";

const Spinner = () => {
  return (
    <div className="spinnerContainer">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
