/* Styles & Img */
import "./Spinner.scss";

const Spinner = ({ styles }) => {
  return (
    <div className={styles}>
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
